name: '05 – 1 – Filters and Activity Types'
on:
  workflow_dispatch:
  # pull_request:
  #   types: [opened, synchronize]
  #   branches:
  #     - main
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: 'Show the trigger'
        run: echo "This workflow was triggered by a ${{ github.event_name }} event."